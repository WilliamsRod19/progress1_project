name: progress1_project
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  rxdart: ^0.27.7
  fluttertoast: ^8.2.2
  http: ^1.6.0
  get_it: ^7.6.4
  injectable: 2.3.2
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  injectable_generator: ^2.4.1
  build_runner: ^2.4.8

flutter:
  uses-material-design: true
  assets:
    - assets/img/
